<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Name="MainWindow"
    Title="Codex 账号管理器"
    Width="760"
    Height="640"
    MinWidth="680"
    MinHeight="500"
    WindowStartupLocation="CenterScreen"
    Background="#FFF4F6F8"
    FontFamily="Microsoft YaHei UI, Segoe UI"
    FontSize="13"
    UseLayoutRounding="True"
    SnapsToDevicePixels="True">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#EFFFFFFF" />
        <SolidColorBrush x:Key="BorderBrush" Color="#BFD8DEE6" />
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FF2563EB" />
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="9" />
            <Setter Property="Padding" Value="12" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect
                        BlurRadius="12"
                        Direction="270"
                        Opacity="0.14"
                        ShadowDepth="2"
                        Color="#FF1F2937" />
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Padding" Value="14,6" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
        </Style>
        <Style x:Key="StatusValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,4,0,0" />
        </Style>
    </Window.Resources>

    <Grid>
        <Image
            x:Name="BackgroundImage"
            Stretch="UniformToFill"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch" />
        <Border x:Name="BackgroundOverlay" Background="#BFFFFFFF" />

        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="12" />
                <RowDefinition Height="*" />
                <RowDefinition Height="12" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="12" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            FontSize="20"
                            FontWeight="SemiBold"
                            VerticalAlignment="Center"
                            Text="Codex 账号管理器" />
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Margin="0,0,8,0" VerticalAlignment="Center" Text="皮肤：" />
                            <ComboBox
                                x:Name="ThemeComboBox"
                                Width="132"
                                MinHeight="30"
                                DisplayMemberPath="Name"
                                SelectedValuePath="Id" />
                        </StackPanel>
                    </Grid>
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,12,0">
                            <TextBlock Foreground="#FF5B6472" Text="Codex 客户端" />
                            <TextBlock x:Name="CodexStatusText" Style="{StaticResource StatusValueStyle}" Text="未知" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,12,0">
                            <TextBlock Foreground="#FF5B6472" Text="当前账号" />
                            <TextBlock x:Name="ActiveProfileText" Style="{StaticResource StatusValueStyle}" Text="未初始化" TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="0,0,12,0">
                            <TextBlock Foreground="#FF5B6472" Text="身份状态" />
                            <TextBlock x:Name="IdentityStatusText" Style="{StaticResource StatusValueStyle}" Text="未检查" />
                        </StackPanel>
                        <StackPanel Grid.Column="3">
                            <TextBlock Foreground="#FF5B6472" Text="网页 ChatGPT" />
                            <TextBlock x:Name="WebChatGPTText" Style="{StaticResource StatusValueStyle}" Text="不受影响" />
                            <TextBlock Margin="0,3,0,0" FontSize="11" Foreground="#FF6B7280" TextWrapping="Wrap" Text="浏览器 / PWA 登录状态不会被本工具修改" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Row="2" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="8" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="8" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0">
                        <TextBlock FontSize="17" FontWeight="SemiBold" Text="已保存账号" />
                        <TextBlock x:Name="ProfileCountText" DockPanel.Dock="Right" Foreground="#FF6B7280" VerticalAlignment="Center" Text="0 个账号" />
                    </DockPanel>
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="260" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,8,0"
                            VerticalAlignment="Center"
                            Text="搜索账号：" />
                        <TextBox
                            x:Name="ProfileSearchTextBox"
                            Grid.Column="1"
                            MinHeight="30"
                            Padding="7,4"
                            ToolTip="仅按本地 Profile 名称搜索，不访问凭证或身份内容" />
                    </Grid>
                    <DataGrid
                        x:Name="ProfilesGrid"
                        Grid.Row="4"
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        CanUserDeleteRows="False"
                        CanUserReorderColumns="True"
                        IsReadOnly="True"
                        HeadersVisibility="Column"
                        GridLinesVisibility="Horizontal"
                        Background="#F2FFFFFF"
                        RowBackground="#EFFFFFFF"
                        AlternatingRowBackground="#E6F7F9FC"
                        BorderBrush="#FFE2E7EE"
                        BorderThickness="1"
                        RowHeaderWidth="0"
                        SelectionMode="Single"
                        SelectionUnit="FullRow"
                        VerticalScrollBarVisibility="Auto"
                        HorizontalScrollBarVisibility="Auto">
                        <DataGrid.ContextMenu>
                            <ContextMenu x:Name="ProfileContextMenu">
                                <MenuItem x:Name="ContextSwitchMenuItem" Header="切换到此账号" />
                                <MenuItem x:Name="ContextVerifyMenuItem" Header="验证账号" />
                                <Separator />
                                <MenuItem x:Name="ContextRenameMenuItem" Header="重命名" />
                                <MenuItem x:Name="ContextDeleteMenuItem" Header="删除账号" />
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Foreground" Value="#FF202733" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Active}" Value="是">
                                        <Setter Property="Background" Value="#FFDCEBFF" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="名称" Binding="{Binding Name}" MinWidth="105" Width="*" />
                            <DataGridTextColumn Header="当前" Binding="{Binding Active}" Width="60" />
                            <DataGridTextColumn Header="验证状态" Binding="{Binding Verification}" Width="82" />
                            <DataGridTextColumn Header="状态" Binding="{Binding Health}" MinWidth="105" Width="Auto">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Padding" Value="6,2" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                        <Setter Property="Foreground" Value="#FF4B5563" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Health}" Value="正常">
                                                <Setter Property="Foreground" Value="#FF167A45" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Health}" Value="不完整">
                                                <Setter Property="Foreground" Value="#FF9A6700" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Health}" Value="元数据异常">
                                                <Setter Property="Foreground" Value="#FFB42318" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="凭证" Binding="{Binding Auth}" Width="72" />
                            <DataGridTextColumn Header="身份标记" Binding="{Binding Identity}" Width="82" />
                            <DataGridTextColumn Header="元数据" Binding="{Binding Metadata}" Width="72" />
                            <DataGridTextColumn Header="更新时间" Binding="{Binding Updated}" MinWidth="170" Width="Auto" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Row="4" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <WrapPanel Grid.Row="0">
                        <Button x:Name="SwitchButton" Content="切换账号" IsEnabled="False" />
                        <Button x:Name="VerifyButton" Content="验证账号" IsEnabled="False" />
                        <Button x:Name="RefreshButton" Content="刷新" Style="{StaticResource PrimaryButtonStyle}" IsDefault="True" />
                    </WrapPanel>
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <WrapPanel Grid.Column="0">
                            <Button x:Name="AddButton" Content="添加账号" IsEnabled="False" />
                            <Button x:Name="RenameButton" Content="重命名" IsEnabled="False" />
                            <Button x:Name="DeleteButton" Content="删除账号" IsEnabled="False" />
                        </WrapPanel>
                        <Button x:Name="ExitCodexButton" Grid.Column="1" Content="正常退出 Codex" IsEnabled="False" Margin="8,0,0,0" />
                    </Grid>
                </Grid>
            </Border>

            <DockPanel Grid.Row="6">
                <TextBlock x:Name="RefreshStatusText" Foreground="#FF374151" FontWeight="SemiBold" Text="准备就绪" />
                <TextBlock DockPanel.Dock="Right" Foreground="#FF4B5563" Text="不访问浏览器或网页登录状态" />
            </DockPanel>
        </Grid>
    </Grid>
</Window>
