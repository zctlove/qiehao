<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Name="MainWindow"
    Title="Codex Account Switcher"
    Width="760"
    Height="560"
    MinWidth="680"
    MinHeight="500"
    WindowStartupLocation="CenterScreen"
    Background="#FFF4F6F8"
    FontFamily="Segoe UI"
    FontSize="13"
    UseLayoutRounding="True"
    SnapsToDevicePixels="True">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="BorderBrush" Color="#FFD8DEE6" />
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FF2563EB" />
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="12" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Padding" Value="14,6" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
        </Style>
        <Style x:Key="StatusValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,4,0,0" />
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource PanelStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Foreground="#FF5B6472" Text="Codex Desktop" />
                    <TextBlock x:Name="CodexStatusText" Style="{StaticResource StatusValueStyle}" Text="Unknown" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                    <TextBlock Foreground="#FF5B6472" Text="Tool Active Profile" />
                    <TextBlock x:Name="ActiveProfileText" Style="{StaticResource StatusValueStyle}" Text="Not initialized" TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="0,0,12,0">
                    <TextBlock Foreground="#FF5B6472" Text="Identity Status" />
                    <TextBlock x:Name="IdentityStatusText" Style="{StaticResource StatusValueStyle}" Text="Not checked" />
                </StackPanel>
                <StackPanel Grid.Column="3">
                    <TextBlock Foreground="#FF5B6472" Text="Web ChatGPT" />
                    <TextBlock x:Name="WebChatGPTText" Style="{StaticResource StatusValueStyle}" Text="Unaffected" />
                    <TextBlock Margin="0,3,0,0" FontSize="11" Foreground="#FF6B7280" TextWrapping="Wrap" Text="Browser / PWA sessions are not managed by this tool." />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource PanelStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <DockPanel Grid.Row="0">
                    <TextBlock FontSize="17" FontWeight="SemiBold" Text="Saved accounts" />
                    <TextBlock x:Name="ProfileCountText" DockPanel.Dock="Right" Foreground="#FF6B7280" VerticalAlignment="Center" Text="0 profiles" />
                </DockPanel>
                <DataGrid
                    x:Name="ProfilesGrid"
                    Grid.Row="2"
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    CanUserReorderColumns="True"
                    IsReadOnly="True"
                    HeadersVisibility="Column"
                    GridLinesVisibility="Horizontal"
                    BorderBrush="#FFE2E7EE"
                    BorderThickness="1"
                    RowHeaderWidth="0"
                    SelectionMode="Single"
                    VerticalScrollBarVisibility="Auto"
                    HorizontalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" MinWidth="105" Width="*" />
                        <DataGridTextColumn Header="Active" Binding="{Binding Active}" Width="65" />
                        <DataGridTextColumn Header="Health" Binding="{Binding Health}" MinWidth="140" Width="Auto">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="6,2" />
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                    <Setter Property="Foreground" Value="#FF4B5563" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Health}" Value="READY">
                                            <Setter Property="Foreground" Value="#FF167A45" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Health}" Value="INCOMPLETE_PROFILE">
                                            <Setter Property="Foreground" Value="#FF9A6700" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Health}" Value="INVALID_METADATA">
                                            <Setter Property="Foreground" Value="#FFB42318" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Auth" Binding="{Binding Auth}" Width="82" />
                        <DataGridTextColumn Header="Identity" Binding="{Binding Identity}" Width="86" />
                        <DataGridTextColumn Header="Metadata" Binding="{Binding Metadata}" Width="86" />
                        <DataGridTextColumn Header="Updated" Binding="{Binding Updated}" MinWidth="170" Width="Auto" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Row="4" Style="{StaticResource PanelStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <WrapPanel Grid.Row="0">
                    <Button x:Name="SwitchButton" Content="Switch Account" IsEnabled="False" />
                    <Button x:Name="VerifyButton" Content="Verify" IsEnabled="False" />
                    <Button x:Name="RefreshButton" Content="Refresh" Style="{StaticResource PrimaryButtonStyle}" IsDefault="True" />
                </WrapPanel>
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <WrapPanel Grid.Column="0">
                        <Button x:Name="AddButton" Content="Add Account" IsEnabled="False" />
                        <Button x:Name="RenameButton" Content="Rename" IsEnabled="False" />
                        <Button x:Name="DeleteButton" Content="Delete" IsEnabled="False" />
                    </WrapPanel>
                    <Button x:Name="ExitCodexButton" Grid.Column="1" Content="Exit Codex Safely" IsEnabled="False" Margin="8,0,0,0" />
                </Grid>
            </Grid>
        </Border>

        <DockPanel Grid.Row="6">
            <TextBlock x:Name="RefreshStatusText" Foreground="#FF5B6472" Text="Read-only GUI phase 1" />
            <TextBlock DockPanel.Dock="Right" Foreground="#FF6B7280" Text="No browser or web session access" />
        </DockPanel>
    </Grid>
</Window>
